[project]
name = "contract-sync-suite"
authors = ["Contract Sync Team"]
description = "Collaborative financial management on Stacks blockchain"
telemetry = false
requirements = []
[contracts.sync-core]
path = "contracts/sync-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
